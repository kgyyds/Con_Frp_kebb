# FRPShell 项目说明

## 项目概述

FRPShell（原名 Con_Frp）是一个基于 FRP（Fast Reverse Proxy）的 Android 远程控制和文件管理应用。该应用通过内网穿透技术，允许用户远程控制和管理 Android 设备，提供了丰富的功能包括 Shell 终端、文件管理、进程管理、屏幕捕获等。

## 基本信息

- **应用名称**: FRPShell
- **包名**: com.kgapp.frpshellpro
- **版本**: 1.0 (版本码: 100)
- **最小 SDK**: 26 (Android 8.0)
- **目标 SDK**: 34 (Android 14)
- **架构支持**: arm64-v8a
- **开发语言**: Kotlin
- **UI 框架**: Jetpack Compose

## 技术栈

### 核心技术
- **Jetpack Compose**: 现代化的 Android UI 框架
- **MVVM 架构**: Model-View-ViewModel 设计模式
- **Kotlin Coroutines**: 异步编程和并发处理
- **StateFlow**: 响应式状态管理
- **Kotlin DSL**: Gradle 构建配置

### 依赖库
```kotlin
// Compose 相关
- androidx.compose:compose-bom:2024.02.00
- androidx.compose.material3:material3
- androidx.compose.material:material-icons-extended

// 生命周期和架构
- androidx.lifecycle:lifecycle-runtime-ktx:2.8.0
- androidx.lifecycle:lifecycle-viewmodel-compose:2.8.0

// 协程
- org.jetbrains.kotlinx:kotlinx-coroutines-android:1.8.0

// 图片加载
- io.coil-kt:coil-compose:2.6.0

// Material Design
- com.google.android.material:material:1.12.0
```

## 项目结构

```
Con_Frp_kebb/
├── app/                          # 应用主模块
│   └── src/main/
│       ├── AndroidManifest.xml    # 应用配置清单
│       ├── assets/               # 资源文件
│       │   ├── frpc             # FRP 客户端二进制 (14.7 MB)
│       │   ├── GetInfo.jar      # 设备信息获取工具
│       │   ├── GetPhoto.jar      # 拍照工具
│       │   ├── GetLoc.apk       # 位置相关应用 (3.2 MB)
│       │   └── upload.sh         # 大文件上传脚本
│       └── java/com/kgapp/frpshellpro/
│           ├── App.kt           # 应用入口
│           ├── MainActivity.kt   # 主 Activity
│           ├── core/            # 核心架构层
│           ├── domain/           # 领域层
│           ├── data/            # 数据层
│           ├── server/          # 网络层
│           ├── frp/             # FRP 管理层
│           ├── model/           # 数据模型
│           └── ui/              # UI 层
```

### 核心架构层 (core/)
- **CommandDispatcherThread.kt**: 命令调度器，负责将 UI 意图转发到网络/FRP 线程
- **CommandModels.kt**: 定义各种命令和事件的数据模型
- **FrpManagerThread.kt**: FRP 管理线程
- **NetworkThread.kt**: 网络通信线程

### 领域层 (domain/usecase/)
- **ShellUseCase.kt**: Shell 命令相关用例
- **FileManagerUseCase.kt**: 文件管理用例
- **ProcessUseCase.kt**: 进程管理用例
- **CaptureUseCase.kt**: 截图和屏幕捕获用例
- **AppListUseCase.kt**: 应用列表获取用例

### 数据层 (data/repository/)
- **DeviceCommandRepository.kt**: 设备命令仓库接口
- **DeviceCommandRepositoryImpl.kt**: 设备命令仓库实现

### 网络层 (server/)
- **TcpServer.kt**: TCP 服务器实现
- **ClientSession.kt**: 客户端会话管理

### FRP 层 (frp/)
- **FrpLogBus.kt**: FRP 日志事件总线
- **FrpManager.kt**: FRP 管理器

### UI 层 (ui/)
- **MainScaffold.kt**: 主要 UI 框架
- **MainViewModel.kt**: 主界面视图模型
- **MainUiState.kt**: 主界面状态定义
- **ShellScreen.kt**: Shell 终端界面
- **FileManagerScreen.kt**: 文件管理界面
- **SettingsScreen.kt**: 设置界面
- **DeviceInfoScreen.kt**: 设备信息界面
- **AppListScreen.kt**: 应用列表界面
- **PerformanceScreen.kt**: 性能监控界面
- **ScreenViewerScreen.kt**: 屏幕查看器界面

## 主要功能

### 1. FRP 反向代理管理
- **功能**: 启动/停止 FRP 服务，实现内网穿透
- **特点**:
  - 支持 su 权限检测和切换
  - 配置文件编辑和管理
  - 自动清理残留进程
  - 日志实时监控

### 2. 远程 Shell 终端
- **功能**: 多客户端终端管理，命令执行和输出显示
- **特点**:
  - 支持多个客户端同时连接
  - 命令历史记录
  - 实时输出显示
  - ANSI 颜色支持

### 3. 文件管理器
- **功能**: 远程文件浏览、上传下载、编辑
- **特点**:
  - 文件浏览和目录导航
  - 文件上传/下载（支持大文件断点续传）
  - 文件在线编辑器
  - 文件权限修改 (chmod)
  - 文件重命名和删除
  - 支持拖拽上传

### 4. 设备信息监控
- **功能**: CPU 频率、内存、磁盘信息监控
- **特点**:
  - 实时 CPU 频率显示
  - 内存使用情况统计
  - 磁盘分区信息
  - 可视化数据展示

### 5. 进程管理
- **功能**: 查看运行中的进程，进程终止
- **特点**:
  - 进程列表显示
  - 按 PID/内存占用排序
  - 进程终止功能
  - 实时刷新

### 6. 应用管理
- **功能**: 获取已安装应用列表
- **特点**:
  - 系统应用和用户应用区分
  - 应用信息展示
  - 使用 scrcpy-server 获取应用列表

### 7. 屏幕捕获
- **功能**: 屏幕截图、相机拍照
- **特点**:
  - 屏幕截图功能
  - 多摄像头支持
  - 图片预览和保存
  - 自定义推流配置

### 8. 录屏功能
- **功能**: 屏幕录制推流
- **特点**:
  - 自定义推流地址和端口
  - 启动/停止模板配置
  - 支持 RTMP 等协议

## 核心设计模式

### 1. 多线程架构
- **网络线程**: 处理所有网络 I/O
- **FRP 线程**: 管理 FRP 进程
- **UI 线程**: 处理用户交互
- **命令调度器**: 协调各线程通信

### 2. 状态管理
- 使用 StateFlow 进行状态管理
- 所有 UI 状态集中管理在 MainUiState 中
- 支持持久化存储 (SettingsStore)

### 3. 错误处理
- 全面的异常捕获和处理
- 友好的错误提示
- 日志记录系统 (FrpLogBus)

## 安全特性

### 1. 权限管理
- `INTERNET`: 网络访问权限
- `FOREGROUND_SERVICE`: 前台服务权限
- su 权限检测和管理

### 2. 安全防护
- 文件操作权限控制
- 命令注入防护
- 会话管理
- 数据加密传输（FRP）

## 应用资源

### 1. 二进制文件
- **frpc**: FRP 客户端 (14.7 MB)
  - 用于建立反向代理连接
  - 支持 TOML 配置文件
  - 自动从 assets 提取

### 2. 工具文件
- **GetInfo.jar**: 设备信息获取工具
  - 获取 CPU、内存、磁盘信息
  - JSON 格式输出

- **GetPhoto.jar**: 拍照工具
  - 相机拍照功能
  - 支持多摄像头

- **GetLoc.apk**: 位置相关应用 (3.2 MB)
  - 位置信息获取

- **scrcpy-server.jar**: 屏幕共享服务
  - 应用列表获取
  - 屏幕录制

### 3. 脚本文件
- **upload.sh**: 大文件上传脚本
  - 支持断点续传
  - 可自定义脚本内容

## 配置说明

### 1. FRP 配置模板
```toml
serverAddr = "your.server.com"
serverPort = 7000

[[proxies]]
name = "example"
type = "tcp"
localIP = "127.0.0.1"
localPort = 22
remotePort = 6000
```

### 2. 录屏配置
- **推流地址**: 默认 `rtmp://localhost:1935/live`
- **推流端口**: 默认 `1935`
- **启动模板**: 可自定义启动命令
- **停止模板**: 可自定义停止命令

### 3. 上传脚本配置
- **脚本路径**: `/data/system/upload.sh`
- **支持大文件断点续传**
- 可通过 UI 自定义脚本内容

## 使用流程

### 1. 初始化设置
1. 首次启动进入设置界面
2. 配置 FRP 服务器地址和端口
3. 选择是否使用 su 权限
4. 设置主题和字体大小

### 2. 启动服务
1. 点击"启动 FRP"按钮
2. 等待连接建立
3. 客户端连接后会显示在列表中

### 3. 远程操作
1. 选择客户端进行连接
2. 使用 Shell 终端执行命令
3. 使用文件管理器管理文件
4. 监控设备性能

## 日志系统

应用包含完整的日志系统：
- **FrpLogBus**: 事件总线，负责日志分发
- **模块分类**: UI、Network、FRP 等模块日志
- **实时显示**: 在界面上实时显示日志
- **持久化**: 重要日志可保存

## 特色设计

### 1. 渐进式功能
- 基础功能：Shell 终端、文件管理
- 高级功能：应用列表、屏幕捕获
- 扩展功能：录屏推流、设备信息

### 2. 用户体验
- Material Design 3 设计
- 深色/浅色主题支持
- 流畅的动画效果
- 直观的操作界面

### 3. 性能优化
- 协程异步处理
- 内存管理优化
- 大文件分块传输
- 连接状态实时监控

## 开发和构建

### 构建配置
```kotlin
android {
    compileSdk = 34
    defaultConfig {
        minSdk = 26
        targetSdk = 34
        versionCode = 100
        versionName = "1.0"
        ndk {
            abiFilters += "arm64-v8a"
        }
    }
}
```

### 构建命令
```bash
./gradlew assembleDebug    # Debug 构建
./gradlew assembleRelease # Release 构建
```

### 签名配置
- 支持自定义签名
- CI/CD 自动签名（通过环境变量）
- 默认使用 debug 签名

## 注意事项

1. **Root 权限**: 某些功能需要 root 权限才能正常工作
2. **网络连接**: FRP 服务需要稳定的网络连接
3. **兼容性**: 支持 Android 8.0 及以上版本
4. **性能**: 大文件传输时注意设备性能
5. **安全**: 建议在可信网络环境下使用

## 更新历史

最近的更新：
- 添加应用列表查看功能
- 优化 UI 界面，添加图标和刷新按钮
- 修复构建错误
- 添加读取通讯录短信的插件

## 总结

FRPShell 是一个功能完整的远程控制和文件管理 Android 应用，采用了现代化的架构设计和技术栈。通过 FRP 实现内网穿透，支持多种远程操作功能。代码结构清晰，遵循 MVVM 模式，具有良好的可维护性和扩展性。应用支持多种 Android 版本，适配 ARM64 架构设备。